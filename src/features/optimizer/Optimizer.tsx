const Optimizer = () => {
  return (
    <div style={{ padding: '2rem', color: 'var(--text-primary)' }}>
      <h1>SVG Code Optimizer</h1>
      <p style={{ marginTop: '1rem', color: 'var(--text-muted)' }}>
        This feature is coming soon. It will help you optimize your SVG code by removing unnecessary characters and simplifying paths.
      </p>
    </div>
  );
};

export default Optimizer;
